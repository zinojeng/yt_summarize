{
  "build": {
    "main": "main.py",
    "buildCommand": "pip install -e .",
    "installCommand": "pip install -r requirements.txt",
    "envs": [
      "OPENAI_API_KEY",
      "GOOGLE_API_KEY",
      "PORT"
    ]
  }
} 